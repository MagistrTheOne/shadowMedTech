app = "shadowmedai-agent"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  USE_SILERO_TTS = "true"
  USE_OPENAI_STT = "true"

[[services]]
  internal_port = 8080
  processes = ["app"]
  protocol = "tcp"

[processes]
  app = "python run_worker.py"

